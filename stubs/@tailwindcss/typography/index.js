export default function typography() {
  return () => ({
    postcssPlugin: "tailwindcss-typography-stub",
    Once() {
      // Typography plugin is stubbed for the local design system seed.
    },
  });
}
