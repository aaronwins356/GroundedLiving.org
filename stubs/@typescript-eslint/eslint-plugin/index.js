// Minimal stub so eslint-config-next can resolve its peer dependency without
// pulling from npm (network installs are blocked in this environment).
module.exports = {
  configs: {},
  rules: {},
};
